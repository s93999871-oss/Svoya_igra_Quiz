<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Викторина EOS — Своя игра</title>

<!-- Шрифты: Unbounded (акцентный) + Montserrat (основной) -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/unbounded/500.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/unbounded/700.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/unbounded/900.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/montserrat/400.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/montserrat/500.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/montserrat/600.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/montserrat/700.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/montserrat/800.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/montserrat/900.css">

<style>
/* ============================================================
   ВИКТОРИНА EOS · СВОЯ ИГРА · DESIGN v6
   ============================================================ */

:root {
  --red-900: #6E0A11;
  --red-700: #A5121B;
  --red-600: #D0181F;
  --red-500: #E01B24;
  --red-400: #EE3B43;

  --gold:      #E7C69A;
  --gold-soft: #FFF8EF;

  --surface:   #F5F5F8;
  --card:      #FFFFFF;
  --ink:       #1A1A1E;
  --ink-soft:  #4A4A55;
  --ink-muted: #8A8A99;
  --line:      #EDEDF2;
  --red-soft:  #FFF3F4;

  --success:      #1E9E5A;
  --success-soft: #E9F8EF;

  --radius: 22px;

  --shadow:      0 10px 30px rgba(30, 12, 14, .07), 0 2px 6px rgba(30, 12, 14, .04);
  --shadow-lift: 0 22px 48px rgba(143, 15, 24, .16);
  --shadow-red:  0 14px 32px rgba(208, 26, 34, .28);

  --gradient-red:  linear-gradient(135deg, var(--red-700), var(--red-500) 55%, var(--red-400));
  --gradient-hero: radial-gradient(120% 150% at 8% 0%, #5E070D 0%, #A5121B 38%, #D8181F 74%, #EA2C33 100%);

  --font-body:    'Montserrat', system-ui, sans-serif;
  --font-display: 'Unbounded', 'Montserrat', sans-serif;

  --ease: cubic-bezier(.22, .61, .36, 1);
}

*, *::before, *::after { box-sizing: border-box; }

body {
  margin: 0;
  min-height: 100vh;
  font-family: var(--font-body);
  background: var(--surface);
  color: var(--ink);
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
}

body::before {
  content: '';
  position: fixed;
  inset: 0;
  z-index: 0;
  pointer-events: none;
  background-image:
    linear-gradient(var(--line) 1px, transparent 1px),
    linear-gradient(90deg, var(--line) 1px, transparent 1px);
  background-size: 44px 44px;
  opacity: .35;
  -webkit-mask-image: linear-gradient(#000 0%, transparent 52%);
          mask-image: linear-gradient(#000 0%, transparent 52%);
}

button { font-family: inherit; cursor: pointer; border: none; background: none; }
::selection { background: rgba(224, 27, 36, .18); }

:focus-visible {
  outline: 3px solid rgba(224, 27, 36, .35);
  outline-offset: 2px;
  border-radius: 6px;
}

.caps {
  font-size: 11px;
  font-weight: 800;
  letter-spacing: .14em;
  text-transform: uppercase;
}

.orb {
  position: fixed;
  z-index: 0;
  border-radius: 50%;
  filter: blur(80px);
  opacity: .45;
  pointer-events: none;
}
.orb--left {
  top: 26vh; left: -180px;
  width: 440px; height: 440px;
  background: #FFD9DC;
  animation: orb-drift-a 18s var(--ease) infinite;
}
.orb--right {
  right: -140px; bottom: -140px;
  width: 380px; height: 380px;
  background: #FFEFDC;
  animation: orb-drift-b 22s var(--ease) infinite;
}

.hero {
  position: relative;
  padding: 52px 20px 78px;
  background: var(--gradient-hero);
  color: #fff;
  text-align: center;
  overflow: hidden;
}
.hero__ring {
  position: absolute;
  border-radius: 50%;
  border: 1px solid rgba(255, 255, 255, .13);
  background: radial-gradient(circle at 35% 35%, rgba(255, 255, 255, .09), transparent 62%);
}
.hero__ring--lg { right: -90px;  bottom: -170px; width: 380px; height: 380px; }
.hero__ring--sm { left: -120px;  top: -140px;    width: 300px; height: 300px; opacity: .7; }

.hero__wave {
  position: absolute;
  left: 50%; bottom: -2px;
  transform: translateX(-50%);
  width: 150%; height: 56px;
  background: var(--surface);
  border-radius: 50% 50% 0 0;
}

.hero__badge {
  display: inline-block;
  padding: 9px 22px;
  border-radius: 999px;
  background: rgba(255, 255, 255, .15);
  border: 1px solid rgba(255, 255, 255, .22);
  backdrop-filter: blur(6px);
}
.hero .caps { font-size: 10.5px; }

.hero__title {
  margin: 26px 0 0;
  font-family: var(--font-display);
  font-size: clamp(26px, 5vw, 46px);
  font-weight: 900;
  letter-spacing: .01em;
  line-height: 1.08;
  text-shadow: 0 14px 34px rgba(0, 0, 0, .22);
}
.hero__title-accent { color: var(--gold); }

.hero__tagline {
  margin: 16px 0 0;
  font-size: 12px;
  font-weight: 700;
  letter-spacing: .12em;
  text-transform: uppercase;
  opacity: .78;
}

.page {
  position: relative;
  z-index: 2;
  max-width: 760px;
  margin: 0 auto;
  padding: 0 22px 96px;
}
.lift { margin-top: -34px; }
.stack { display: flex; flex-direction: column; gap: 16px; }

.screen { display: none; }
.screen.is-active {
  display: block;
  animation: rise .55s var(--ease) both;
}

.card {
  padding: 36px;
  background: var(--card);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
}
.card--flush { padding: 0; overflow: hidden; }
.card--accent {
  border-left: 4px solid transparent;
  background-image: linear-gradient(var(--card), var(--card)), var(--gradient-red);
  background-origin: border-box;
  background-clip: padding-box, border-box;
}

.title {
  margin: 0 0 10px;
  font-size: 23px;
  font-weight: 900;
  letter-spacing: -.01em;
}
.subtitle {
  margin: 0 0 28px;
  font-size: 13.5px;
  font-weight: 500;
  line-height: 1.7;
  color: var(--ink-muted);
}

/* ---------- Кнопка-подсказка (фиксированная) ---------- */
.help-btn {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 100;
  width: 44px;
  height: 44px;
  border-radius: 50%;
  background: #fff;
  border: 1.5px solid var(--line);
  box-shadow: var(--shadow);
  font-size: 20px;
  font-weight: 900;
  color: var(--red-600);
  cursor: pointer;
  transition: all .3s var(--ease);
  display: grid;
  place-items: center;
}
.help-btn:hover {
  transform: scale(1.1);
  box-shadow: var(--shadow-lift);
  border-color: var(--red-400);
  background: var(--red-soft);
}

/* ---------- Модальное окно ---------- */
.modal-overlay {
  position: fixed;
  inset: 0;
  z-index: 200;
  background: rgba(0, 0, 0, .5);
  display: none;
  align-items: center;
  justify-content: center;
  padding: 20px;
  animation: fadeIn .3s var(--ease);
}
.modal-overlay.is-open {
  display: flex;
}
.modal {
  max-width: 480px;
  width: 100%;
  max-height: 80vh;
  overflow-y: auto;
  background: #fff;
  border-radius: var(--radius);
  box-shadow: var(--shadow-lift);
  padding: 32px;
  animation: modalIn .4s var(--ease);
}
.modal__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
}
.modal__title {
  font-family: var(--font-display);
  font-size: 20px;
  font-weight: 700;
  margin: 0;
}
.modal__close {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: var(--surface);
  border: 1px solid var(--line);
  font-size: 16px;
  font-weight: 700;
  color: var(--ink-muted);
  cursor: pointer;
  transition: all .3s var(--ease);
  display: grid;
  place-items: center;
}
.modal__close:hover {
  background: var(--red-soft);
  color: var(--red-600);
  border-color: var(--red-400);
}
.modal__section {
  margin-bottom: 20px;
}
.modal__section:last-child {
  margin-bottom: 0;
}
.modal__section-title {
  font-size: 14px;
  font-weight: 800;
  color: var(--red-600);
  margin-bottom: 6px;
  letter-spacing: .02em;
}
.modal__section-text {
  font-size: 13px;
  font-weight: 500;
  line-height: 1.65;
  color: var(--ink-soft);
}

/* ---------- Навигация ---------- */
.nav-tabs {
  display: flex;
  gap: 8px;
  margin-bottom: 20px;
  background: #fff;
  border-radius: 999px;
  padding: 6px;
  box-shadow: var(--shadow);
}
.nav-tab {
  flex: 1;
  padding: 12px 16px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 800;
  letter-spacing: .06em;
  text-transform: uppercase;
  color: var(--ink-soft);
  transition: all .3s var(--ease);
  text-align: center;
}
.nav-tab:hover {
  background: var(--red-soft);
  color: var(--red-600);
}
.nav-tab.is-active {
  background: var(--gradient-red);
  color: #fff;
  box-shadow: var(--shadow-red);
}

/* ---------- Вкладки месяцев ---------- */
.months-tabs {
  display: flex;
  gap: 12px;
  margin-bottom: 20px;
}
.month-tab {
  flex: 1;
  position: relative;
  padding: 16px;
  border-radius: 18px;
  background: #fff;
  border: 1.5px solid var(--line);
  text-align: center;
  transition: all .3s var(--ease);
  cursor: pointer;
}
.month-tab:hover:not(.is-locked) {
  transform: translateY(-3px);
  box-shadow: var(--shadow-lift);
  border-color: var(--red-400);
}
.month-tab.is-active {
  background: var(--gradient-red);
  color: #fff;
  border-color: transparent;
  box-shadow: var(--shadow-red);
}
.month-tab.is-locked {
  opacity: .6;
  cursor: not-allowed;
}
.month-tab__name {
  font-family: var(--font-display);
  font-size: 16px;
  font-weight: 700;
  text-transform: uppercase;
}
.month-tab__year {
  font-size: 11px;
  font-weight: 600;
  opacity: .7;
  margin-top: 4px;
}
.month-tab__status {
  display: inline-block;
  margin-top: 8px;
  padding: 3px 10px;
  border-radius: 999px;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: .1em;
  text-transform: uppercase;
}
.month-tab.is-active .month-tab__status {
  background: rgba(255, 255, 255, .2);
  color: #fff;
}
.month-tab.is-locked .month-tab__status {
  background: #EDEDF2;
  color: var(--ink-muted);
}

.month-tab__tooltip {
  position: absolute;
  bottom: 100%;
  left: 50%;
  transform: translateX(-50%) translateY(10px);
  padding: 10px 16px;
  background: var(--ink);
  color: #fff;
  font-size: 11px;
  font-weight: 600;
  border-radius: 10px;
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: all .3s var(--ease);
  z-index: 10;
}
.month-tab__tooltip::after {
  content: '';
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%);
  border: 6px solid transparent;
  border-top-color: var(--ink);
}
.month-tab.is-locked:hover .month-tab__tooltip {
  opacity: 1;
  transform: translateX(-50%) translateY(0);
}

/* ---------- Старт ---------- */
.rules { margin: 0 0 30px; padding: 0; list-style: none; }
.rules__item {
  display: flex;
  gap: 16px;
  align-items: baseline;
  padding: 16px 0;
  border-bottom: 1px solid var(--line);
}
.rules__item:first-child { padding-top: 0; }
.rules__item:last-child  { border: none; padding-bottom: 0; }
.rules__label {
  min-width: 84px;
  font-size: 10.5px;
  font-weight: 800;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--red-600);
}
.rules__text { font-size: 13.5px; font-weight: 500; line-height: 1.6; color: var(--ink-soft); }

.field {
  width: 100%;
  padding: 17px 22px;
  font: 500 14.5px var(--font-body);
  color: var(--ink);
  background: #FAFAFC;
  border: 1.5px solid var(--line);
  border-radius: 999px;
  box-shadow: inset 0 1px 3px rgba(0, 0, 0, .03);
  outline: none;
  transition: border-color .3s var(--ease), box-shadow .3s var(--ease), background .3s var(--ease);
}
.field:focus {
  background: #fff;
  border-color: var(--red-400);
  box-shadow: 0 0 0 4px rgba(224, 27, 36, .10);
}
.field--invalid {
  border-color: var(--red-500);
  box-shadow: 0 0 0 4px rgba(224, 27, 36, .10);
  animation: shake .5s var(--ease);
}
.field-error {
  display: none;
  margin: 10px 4px 0;
  font-size: 12px;
  font-weight: 600;
  color: var(--red-500);
}
.field-error.is-visible { display: block; }

.actions { display: flex; flex-direction: column; gap: 14px; margin-top: 22px; }
.actions--flush { margin-top: 0; }

.btn {
  position: relative;
  overflow: hidden;
  width: 100%;
  padding: 18px;
  border-radius: 999px;
  background: var(--gradient-red);
  color: #fff;
  font-size: 14px;
  font-weight: 800;
  letter-spacing: .06em;
  text-transform: uppercase;
  box-shadow: var(--shadow-red);
  transition: transform .35s var(--ease), box-shadow .35s var(--ease), background .35s var(--ease);
}
.btn::after {
  content: '';
  position: absolute;
  top: 0; left: -80%;
  width: 50%; height: 100%;
  background: linear-gradient(100deg, transparent, rgba(255, 255, 255, .35), transparent);
  transform: skewX(-20deg);
  transition: left .6s var(--ease);
}
.btn:hover { transform: translateY(-3px); box-shadow: 0 20px 40px rgba(208, 26, 34, .34); }
.btn:hover::after { left: 130%; }
.btn:active { transform: translateY(-1px) scale(.985); }

.btn--ghost {
  background: #fff;
  color: var(--red-600);
  border: 1.5px solid #FFD3D6;
  box-shadow: var(--shadow);
}
.btn--ghost::after { background: linear-gradient(100deg, transparent, rgba(224, 27, 36, .08), transparent); }
.btn--ghost:hover { background: var(--red-soft); box-shadow: 0 14px 30px rgba(208, 26, 34, .13); }

/* ---------- Вопрос ---------- */
.quiz-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 22px 26px;
  background: var(--gradient-hero);
  color: #fff;
}
.quiz-bar__counter { opacity: .85; }

.timer {
  position: relative;
  display: grid;
  place-items: center;
  width: 80px; height: 80px;
  border-radius: 50%;
  background: rgba(255, 255, 255, .12);
  border: 1px solid rgba(255, 255, 255, .2);
  transition: background .3s var(--ease), box-shadow .3s var(--ease);
}
.timer__ring { position: absolute; inset: 4px; transform: rotate(-90deg); }
.timer__ring circle { fill: none; stroke-width: 4; stroke-linecap: round; }
.timer__ring-bg   { stroke: rgba(255, 255, 255, .22); }
.timer__ring-fill { stroke: #fff; }
.timer__inner { position: relative; text-align: center; }
.timer__value { font-family: var(--font-display); font-size: 21px; font-weight: 700; line-height: 1; }
.timer__label { margin-top: 3px; font-size: 8px; font-weight: 700; letter-spacing: .16em; opacity: .75; text-transform: uppercase; }

.timer--warn {
  background: #fff;
  box-shadow: 0 10px 26px rgba(224, 27, 36, .35);
  animation: pulse 1s infinite;
}
.timer--warn .timer__ring-bg   { stroke: #F4CDD0; }
.timer--warn .timer__ring-fill { stroke: var(--red-500); }
.timer--warn .timer__value     { color: var(--red-600); }
.timer--warn .timer__label     { color: var(--red-600); opacity: .8; }

.quiz-body { position: relative; padding: 34px 30px 36px; }
.question {
  margin: 0;
  font-size: clamp(18px, 3.4vw, 23px);
  font-weight: 800;
  line-height: 1.45;
}

.options {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-top: 30px;
}
.option {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 14px;
  min-height: 118px;
  padding: 22px 20px;
  text-align: left;
  font-size: 14px;
  font-weight: 600;
  line-height: 1.5;
  color: var(--ink);
  background: #fff;
  border: 1.5px solid var(--line);
  border-radius: 18px;
  box-shadow: var(--shadow);
  transition: transform .32s var(--ease), background .32s var(--ease),
              border-color .32s var(--ease), box-shadow .32s var(--ease), opacity .32s var(--ease);
}
.option:hover:not(:disabled) {
  transform: translateY(-5px);
  background: var(--red-soft);
  border-color: #FFCACE;
  box-shadow: var(--shadow-lift);
}
.option:active:not(:disabled) { transform: translateY(-2px) scale(.98); }
.option:disabled { cursor: default; }

.option__key {
  flex: none;
  display: grid;
  place-items: center;
  width: 28px; height: 28px;
  border-radius: 50%;
  background: var(--red-soft);
  color: var(--red-600);
  font-size: 12px;
  font-weight: 900;
  transition: background .3s var(--ease), color .3s var(--ease);
}
.option__text { padding-right: 30px; }

.option__icon {
  position: absolute;
  top: 16px; right: 16px;
  display: grid;
  place-items: center;
  width: 26px; height: 26px;
  border-radius: 50%;
  font-size: 13px;
  font-weight: 900;
  opacity: 0;
  transform: scale(.4);
  transition: opacity .35s var(--ease), transform .35s var(--ease);
}
.option--correct .option__icon { opacity: 1; transform: scale(1); background: var(--success); color: #fff; }
.option--correct .option__icon::before { content: '✓'; }
.option--wrong .option__icon   { opacity: 1; transform: scale(1); background: var(--red-500); color: #fff; }
.option--wrong .option__icon::before { content: '✕'; }

.option--selected { background: var(--red-soft); border-color: var(--red-400); box-shadow: var(--shadow-lift); }
.option--selected .option__key { background: var(--red-500); color: #fff; }

.option--correct { background: var(--success-soft); border-color: var(--success); animation: pop .45s var(--ease); }
.option--correct .option__key { background: var(--success); color: #fff; }
.option--wrong { background: var(--red-soft); border-color: var(--red-500); animation: shake .5s var(--ease); }
.option--wrong .option__key { background: var(--red-500); color: #fff; }
.option--dim { opacity: .4; }

.quiz-hint {
  margin: 22px 0 0;
  text-align: center;
  font-size: 11px;
  font-weight: 600;
  letter-spacing: .04em;
  color: var(--ink-muted);
}

.toast {
  position: absolute;
  top: 12px; left: 50%;
  transform: translate(-50%, -14px);
  z-index: 5;
  padding: 10px 22px;
  border-radius: 999px;
  white-space: nowrap;
  color: #fff;
  font-size: 12px;
  font-weight: 800;
  letter-spacing: .06em;
  opacity: 0;
  pointer-events: none;
  transition: opacity .35s var(--ease), transform .35s var(--ease);
}
.toast.is-visible { opacity: 1; transform: translate(-50%, 0); }
.toast--ok   { background: var(--success);  box-shadow: 0 10px 24px rgba(30, 158, 90, .35); }
.toast--fail { background: var(--red-500);  box-shadow: 0 10px 24px rgba(224, 27, 36, .35); }
.toast--warn { background: var(--ink);      box-shadow: 0 10px 24px rgba(0, 0, 0, .25); }

.progress { display: flex; gap: 6px; margin-top: 24px; }
.progress__seg {
  flex: 1;
  height: 6px;
  border-radius: 99px;
  background: #DFDFE7;
  transition: background .4s var(--ease);
}
.progress__seg--ok      { background: var(--success); }
.progress__seg--fail    { background: var(--red-500); }
.progress__seg--current {
  background: linear-gradient(90deg, var(--red-400), var(--red-600));
  animation: seg-pulse 1.6s infinite;
}

/* ---------- Результат ---------- */
.result {
  padding: 42px 34px;
  color: #fff;
  background: var(--gradient-red);
  border-radius: var(--radius);
  box-shadow: var(--shadow-red);
}
.result__caption { text-align: center; opacity: .8; }

.result__grid {
  display: grid;
  grid-template-columns: 150px 1fr;
  gap: 30px;
  align-items: center;
  margin-top: 28px;
  text-align: left;
}

.acc { position: relative; width: 150px; height: 150px; }
.acc svg { position: absolute; inset: 0; transform: rotate(-90deg); }
.acc circle { fill: none; stroke-width: 9; stroke-linecap: round; }
.acc__bg   { stroke: rgba(255, 255, 255, .18); }
.acc__fill { stroke: #fff; }
.acc__center {
  position: absolute;
  inset: 0;
  display: grid;
  place-content: center;
  text-align: center;
}
.acc__num { font-family: var(--font-display); font-size: 30px; font-weight: 900; }
.acc__label { margin-top: 5px; font-size: 9px; font-weight: 700; letter-spacing: .16em; text-transform: uppercase; opacity: .8; }

.result__grade {
  font-family: var(--font-display);
  font-size: clamp(20px, 3.6vw, 27px);
  font-weight: 700;
  line-height: 1.25;
}
.result__note { margin-top: 10px; font-size: 13px; font-weight: 500; line-height: 1.65; opacity: .88; }
.result__name { margin-top: 14px; color: var(--gold); font-size: 14px; font-weight: 700; letter-spacing: .02em; }

.stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14px;
  margin-top: 28px;
}
.stat {
  padding: 18px 8px;
  border-radius: 16px;
  background: rgba(255, 255, 255, .13);
  border: 1px solid rgba(255, 255, 255, .18);
  text-align: center;
}
.stat__value { font-family: var(--font-display); font-size: 20px; font-weight: 700; }
.stat__sep { opacity: .55; font-size: 15px; }
.stat__label {
  margin-top: 6px;
  font-size: 8.5px;
  font-weight: 700;
  letter-spacing: .12em;
  text-transform: uppercase;
  opacity: .82;
}

/* ---------- Пьедестал ---------- */
.podium {
  display: flex;
  gap: 14px;
  align-items: flex-end;
  margin-top: 42px;
}
.podium__col {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.podium__card {
  position: relative;
  width: 100%;
  padding: 20px 10px 16px;
  text-align: center;
  background: #fff;
  border: 1.5px solid var(--line);
  border-radius: 18px;
  box-shadow: var(--shadow);
  animation: rise .6s var(--ease) both;
  transition: transform .35s var(--ease), box-shadow .35s var(--ease);
}
.podium__card:hover { transform: translateY(-4px); box-shadow: var(--shadow-lift); }
.podium__col--1 .podium__card {
  border-color: #EAD9BC;
  background: linear-gradient(#FFFDF8, #fff);
  animation-delay: .12s;
}
.podium__col--3 .podium__card { animation-delay: .24s; }

.podium__medal {
  position: absolute;
  top: -13px; left: 50%;
  transform: translateX(-50%);
  width: 26px; height: 26px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  font-size: 11px;
  font-weight: 900;
  color: #5C430F;
  background: linear-gradient(135deg, #FFE9B8, #E8BC66);
  box-shadow: 0 4px 10px rgba(190, 150, 60, .4);
}
.podium__medal--2 { background: linear-gradient(135deg, #F2F3F6, #C3C7D1); color: #555A66; box-shadow: 0 4px 10px rgba(140, 145, 160, .35); }
.podium__medal--3 { background: linear-gradient(135deg, #F3D3B3, #D89A62); color: #6B4318; box-shadow: 0 4px 10px rgba(190, 130, 80, .35); }

.podium__ava {
  width: 46px; height: 46px;
  margin: 8px auto 9px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  font-size: 14px;
  font-weight: 900;
  color: #fff;
  background: var(--gradient-red);
}
.podium__col--2 .podium__ava { background: linear-gradient(135deg, #9BA0AE, #C9CDD6); }
.podium__col--3 .podium__ava { background: linear-gradient(135deg, #C98A57, #E6B98C); }

.podium__name { font-size: 12px; font-weight: 800; line-height: 1.35; overflow-wrap: anywhere; }
.podium__meta { margin-top: 6px; font-size: 10.5px; font-weight: 600; color: var(--ink-muted); }
.podium__meta b { color: var(--ink); font-size: 12px; }
.podium__tag {
  display: inline-block;
  margin-top: 8px;
  padding: 3px 9px;
  border-radius: 999px;
  background: var(--red-500);
  color: #fff;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: .1em;
  text-transform: uppercase;
}

.podium__bar { width: 78%; margin-top: 10px; border-radius: 10px 10px 0 0; }
.podium__col--1 .podium__bar { height: 104px; background: var(--gradient-red); opacity: .92; }
.podium__col--2 .podium__bar { height: 70px;  background: linear-gradient(180deg, #C9CDD6, #EDEFF3); }
.podium__col--3 .podium__bar { height: 48px;  background: linear-gradient(180deg, #E0A874, #F3DCC8); }

/* ---------- Таблица ---------- */
.board { margin-top: 30px; }
.board__row {
  display: grid;
  grid-template-columns: 46px 1fr 64px 64px;
  gap: 12px;
  align-items: center;
  padding: 16px 14px;
  border-bottom: 1px solid var(--line);
  border-radius: 12px;
  font-size: 13.5px;
  font-weight: 600;
}
.board__row--head {
  padding: 12px 14px;
  border-bottom: 1.5px solid var(--line);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--red-600);
}
.board__place { font-weight: 900; color: var(--ink-muted); text-align: center; }
.board__num   { font-weight: 700; color: var(--ink-soft); text-align: center; }
.board__row--mine { background: var(--red-soft); box-shadow: inset 3px 0 0 var(--red-500); }
.board__tag {
  display: inline-block;
  margin-left: 8px;
  padding: 3px 9px;
  border-radius: 999px;
  background: var(--red-500);
  color: #fff;
  font-size: 9px;
  font-weight: 800;
  letter-spacing: .1em;
  text-transform: uppercase;
}

/* ---------- Архив ---------- */
.archive-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 12px;
  margin-top: 20px;
}
.archive-month {
  padding: 20px;
  text-align: center;
  background: #fff;
  border: 1.5px solid var(--line);
  border-radius: 18px;
  cursor: pointer;
  transition: all .3s var(--ease);
}
.archive-month:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lift);
  border-color: var(--red-400);
}
.archive-month__name {
  font-family: var(--font-display);
  font-size: 16px;
  font-weight: 700;
  text-transform: uppercase;
}
.archive-month__year {
  font-size: 11px;
  color: var(--ink-muted);
  margin-top: 4px;
}

/* ---------- Конфетти ---------- */
.confetti {
  position: fixed;
  top: -20px;
  z-index: 99;
  width: 9px; height: 9px;
  pointer-events: none;
  animation: confetti-fall 3s linear forwards;
}

/* ---------- Анимации ---------- */
@keyframes rise { from { opacity: 0; transform: translateY(22px); } }
@keyframes pulse { 50% { transform: scale(1.06); } }
@keyframes seg-pulse { 50% { opacity: .5; } }
@keyframes orb-drift-a { 50% { transform: translate(60px, -40px); } }
@keyframes orb-drift-b { 50% { transform: translate(-50px, 40px); } }
@keyframes confetti-fall { to { transform: translateY(105vh) rotate(680deg); opacity: 0; } }
@keyframes shake {
  20% { transform: translateX(-7px); }
  40% { transform: translateX(6px); }
  60% { transform: translateX(-4px); }
  80% { transform: translateX(3px); }
}
@keyframes pop {
  0%   { transform: scale(1); }
  45%  { transform: scale(1.03); }
  100% { transform: scale(1); }
}
@keyframes fadeIn { from { opacity: 0; } }
@keyframes modalIn { from { opacity: 0; transform: translateY(20px); } }

/* ---------- Адаптивность ---------- */
@media (max-width: 560px) {
  .hero { padding: 42px 20px 64px; }
  .card { padding: 26px; }
  .quiz-body { padding: 26px 22px 28px; }
  .quiz-bar { padding: 18px 20px; }
  .result { padding: 32px 22px; }

  .help-btn {
    top: 12px;
    right: 12px;
    width: 40px;
    height: 40px;
    font-size: 18px;
  }

  .nav-tabs { gap: 4px; padding: 4px; }
  .nav-tab { padding: 10px 8px; font-size: 9px; }

  .months-tabs { gap: 8px; }
  .month-tab { padding: 12px; }
  .month-tab__name { font-size: 14px; }

  .options { grid-template-columns: 1fr; gap: 12px; }
  .option {
    flex-direction: row;
    align-items: center;
    gap: 14px;
    min-height: auto;
    padding: 18px;
  }
  .option__text { padding-right: 36px; }
  .option__icon { top: 50%; right: 14px; transform: translateY(-50%) scale(.4); }
  .option--correct .option__icon,
  .option--wrong .option__icon { transform: translateY(-50%) scale(1); }

  .result__grid { grid-template-columns: 1fr; justify-items: center; gap: 22px; text-align: center; }
  .result__grade { font-size: 19px; }

  .podium { gap: 8px; margin-top: 36px; }
  .podium__ava { width: 38px; height: 38px; font-size: 12px; }
  .podium__name { font-size: 10.5px; }
  .podium__meta { font-size: 9px; }
  .podium__col--1 .podium__bar { height: 72px; }
  .podium__col--2 .podium__bar { height: 48px; }
  .podium__col--3 .podium__bar { height: 32px; }

  .board__row {
    grid-template-columns: 36px 1fr 44px 52px;
    padding: 14px 10px;
    font-size: 12.5px;
  }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: .01ms !important;
  }
}
</style>
</head>
<body>

<div class="orb orb--left"></div>
<div class="orb orb--right"></div>

<!-- Кнопка-подсказка (фиксированная) -->
<button class="help-btn" id="help-btn" aria-label="Помощь">?</button>

<!-- Модальное окно -->
<div class="modal-overlay" id="modal-overlay">
  <div class="modal">
    <div class="modal__header">
      <h3 class="modal__title">Как устроена викторина</h3>
      <button class="modal__close" id="modal-close" aria-label="Закрыть">✕</button>
    </div>
    <div class="modal__section">
      <div class="modal__section-title">Викторина месяца</div>
      <div class="modal__section-text">Здесь ты проходишь викторину текущего месяца. Заполни имя, ответь на вопросы и заработай очки в общий рейтинг. Засчитывается только первое прохождение.</div>
    </div>
    <div class="modal__section">
      <div class="modal__section-title">Общий рейтинг</div>
      <div class="modal__section-text">Сумма твоих очков за все месяцы. Чем больше участвуешь, тем выше поднимаешься.</div>
    </div>
    <div class="modal__section">
      <div class="modal__section-title">Архив</div>
      <div class="modal__section-text">Здесь хранятся рейтинги прошедших викторин. Загляни, чтобы вспомнить, кто был лучшим в прошлые месяцы.</div>
    </div>
  </div>
</div>

<header class="hero">
  <div class="hero__ring hero__ring--lg"></div>
  <div class="hero__ring hero__ring--sm"></div>

  <div class="hero__badge"><span class="caps">Своя игра EOS · 2026</span></div>
  <h1 class="hero__title">ВИКТОРИНА <span class="hero__title-accent">EOS</span></h1>
  <p class="hero__tagline">Отвечай на вопросы месяца и зарабатывай очки в общий рейтинг</p>

  <div class="hero__wave"></div>
</header>

<main class="page">

  <!-- ===================== НАВИГАЦИЯ ===================== -->
  <div class="nav-tabs lift" id="nav-tabs">
    <button class="nav-tab is-active" data-nav="quiz">Викторина месяца</button>
    <button class="nav-tab" data-nav="overall">Общий рейтинг</button>
    <button class="nav-tab" data-nav="archive">Архив</button>
  </div>

  <!-- ===================== ВКЛАДКИ МЕСЯЦЕВ ===================== -->
  <div class="months-tabs" id="months-tabs"></div>

  <!-- ===================== СТАРТ ===================== -->
  <section class="screen is-active" data-screen="start">
    <div class="card card--accent">
      <h2 class="title">Проверь свои знания</h2>
      <p class="subtitle" id="quiz-subtitle">Ответь на вопросы месяца и займи место в рейтинге.</p>

      <ul class="rules">
        <li class="rules__item">
          <b class="rules__label">Шаг 1</b>
          <span class="rules__text">Представься: укажи фамилию и имя</span>
        </li>
        <li class="rules__item">
          <b class="rules__label">Шаг 2</b>
          <span class="rules__text" id="rules-questions">Ответь на вопросы месяца</span>
        </li>
        <li class="rules__item">
          <b class="rules__label">Шаг 3</b>
          <span class="rules__text" id="rules-time">На каждый вопрос — 25 секунд</span>
        </li>
        <li class="rules__item">
          <b class="rules__label">Шаг 4</b>
          <span class="rules__text">Вопрос нельзя пропустить — ответь или время выйдет</span>
        </li>
        <li class="rules__item">
          <b class="rules__label">Важно</b>
          <span class="rules__text">Засчитывается только первое прохождение</span>
        </li>
      </ul>

      <input class="field" id="player-name" placeholder="Фамилия и имя — например, Иванов Иван" autocomplete="off">
      <p class="field-error" id="name-error">Укажи фамилию и имя полностью</p>

      <div class="actions">
        <button class="btn" id="btn-start">Начать викторину</button>
        <button class="btn btn--ghost" id="btn-month-leaderboard">Рейтинг месяца</button>
      </div>
    </div>
  </section>

  <!-- ===================== ВОПРОС ===================== -->
  <section class="screen" data-screen="quiz">
    <div class="card card--flush">
      <div class="quiz-bar">
        <div class="quiz-bar__counter caps">Вопрос <span id="q-number">1</span> из <span id="q-total">10</span></div>

        <div class="timer" id="timer-box" aria-live="polite">
          <svg class="timer__ring" viewBox="0 0 72 72">
            <circle class="timer__ring-bg"   cx="36" cy="36" r="32"></circle>
            <circle class="timer__ring-fill" id="timer-ring" cx="36" cy="36" r="32"></circle>
          </svg>
          <div class="timer__inner">
            <div class="timer__value" id="timer-value">25</div>
            <div class="timer__label">сек</div>
          </div>
        </div>
      </div>

      <div class="quiz-body">
        <div class="toast" id="toast"></div>
        <p class="question" id="question-text">—</p>
        <div class="options" id="options" aria-label="Варианты ответа"></div>
        <p class="quiz-hint">Кликни на вариант дважды для подтверждения ответа</p>
      </div>
    </div>

    <div class="progress" id="progress" aria-hidden="true"></div>
  </section>

  <!-- ===================== РЕЗУЛЬТАТ ===================== -->
  <section class="screen" data-screen="result">
    <div class="stack">
      <div class="result">
        <div class="result__caption caps">Викторина завершена — вот твой результат</div>

        <div class="result__grid">
          <div class="acc">
            <svg viewBox="0 0 120 120">
              <circle class="acc__bg"   cx="60" cy="60" r="52"></circle>
              <circle class="acc__fill" id="acc-fill" cx="60" cy="60" r="52"></circle>
            </svg>
            <div class="acc__center">
              <div class="acc__num" id="acc-num">0%</div>
              <div class="acc__label">точность</div>
            </div>
          </div>

          <div class="result__info">
            <div class="result__grade" id="res-grade">—</div>
            <div class="result__note" id="res-note">—</div>
            <div class="result__name" id="res-name">—</div>
          </div>
        </div>

        <div class="stats">
          <div class="stat">
            <div class="stat__value"><span id="res-score">0</span><span class="stat__sep" id="res-total">/10</span></div>
            <div class="stat__label">верных ответов</div>
          </div>
          <div class="stat"><div class="stat__value" id="stat-time">0:00</div><div class="stat__label">время игры</div></div>
          <div class="stat"><div class="stat__value" id="stat-streak">0</div><div class="stat__label">лучшая серия</div></div>
        </div>
      </div>

      <div class="actions actions--flush">
        <button class="btn" id="btn-to-board">Открыть рейтинг месяца</button>
      </div>
    </div>
  </section>

  <!-- ===================== РЕЙТИНГ МЕСЯЦА ===================== -->
  <section class="screen" data-screen="leaderboard">
    <div class="card">
      <h2 class="title" id="leaderboard-title">Рейтинг месяца</h2>
      <p class="subtitle">Топ-3 на пьедестале. Все участники — в таблице. При равных очках побеждает скорость.</p>

      <div class="podium" id="podium"></div>

      <div class="board">
        <div class="board__row board__row--head">
          <div>Место</div>
          <div>Участник</div>
          <div class="board__num">Верно</div>
          <div class="board__num">Время</div>
        </div>
        <div id="board-rows"></div>
      </div>

      <div class="actions">
        <button class="btn btn--ghost" id="btn-back-from-leaderboard">Назад</button>
      </div>
    </div>
  </section>

  <!-- ===================== ОБЩИЙ РЕЙТИНГ ===================== -->
  <section class="screen" data-screen="overall">
    <div class="card">
      <h2 class="title">Общий рейтинг</h2>
      <p class="subtitle">Сумма очков за все месяцы. Участвуй каждый месяц, чтобы подняться выше!</p>

      <div class="podium" id="overall-podium"></div>

      <div class="board">
        <div class="board__row board__row--head">
          <div>Место</div>
          <div>Участник</div>
          <div class="board__num">Очки</div>
          <div class="board__num">Игр</div>
        </div>
        <div id="overall-rows"></div>
      </div>

      <div class="actions">
        <button class="btn btn--ghost" id="btn-back-from-overall">Назад</button>
      </div>
    </div>
  </section>

  <!-- ===================== АРХИВ ===================== -->
  <section class="screen" data-screen="archive">
    <div class="card">
      <h2 class="title">Архив викторин</h2>
      <p class="subtitle">Выбери месяц, чтобы посмотреть рейтинг.</p>

      <div class="archive-grid" id="archive-grid"></div>

      <div class="actions">
        <button class="btn btn--ghost" id="btn-back-from-archive">Назад</button>
      </div>
    </div>
  </section>

</main>

<script>
(() => {
  'use strict';

  const TIMEOUT = -1;
  const TIMER_RADIUS = 32;
  const TIMER_C = 2 * Math.PI * TIMER_RADIUS;
  const ACC_RADIUS = 52;
  const ACC_C = 2 * Math.PI * ACC_RADIUS;

  const GOOGLE_SHEETS_URL = 'https://script.google.com/macros/s/AKfycbwSs3bZO5rC1ioJJ4Gc5Eo2P0wRY-UsgM8sDCM3seFmAOUe5S7BeCqesjqhYlYsZaGzlg/exec';

  const CONFIG = {
    questionsPerGame: 10,
    secondsPerQuestion: 25,
    warningThreshold: 5,
    answerRevealDelay: 950,
    passThreshold: 75,
    podiumSize: 3,
    answerLetters: ['А', 'Б', 'В', 'Г'],
    namePattern: /^[А-Яа-яЁёA-Za-z][А-Яа-яЁёA-Za-z-]*\s+[А-Яа-яЁёA-Za-z][А-Яа-яЁёA-Za-z-]*$/,
  };

  const GRADES = Object.freeze([
    { min: 100, title: 'Идеально!', note: 'Все ответы верные — ты отлично знаешь этот месяц!' },
    { min: 75,  title: 'Отличный результат', note: 'Совсем чуть-чуть до совершенства!' },
    { min: 50,  title: 'Хороший уровень', note: 'Половина месяца у тебя в кармане.' },
    { min: 0,   title: 'Будь внимательнее', note: 'В следующем месяце точно получится лучше!' },
  ]);

  const TOASTS = Object.freeze({
    correct: 'Верно! +1',
    wrong: 'Неверно — правильный ответ подсвечен',
    timeout: 'Время! Правильный ответ подсвечен',
  });

  const state = {
    settings: null,
    months: [],
    activeMonth: null,
    nextMonth: null,
    questions: [],
    player: '',
    index: 0,
    score: 0,
    spentSeconds: 0,
    timeLeft: CONFIG.secondsPerQuestion,
    timerId: null,
    locked: false,
    selected: null,
    history: [],
    streak: 0,
    bestStreak: 0,
    answers: [],
    toastId: null,
  };

  const $ = (selector, scope = document) => scope.querySelector(selector);
  const $$ = (selector, scope = document) => [...scope.querySelectorAll(selector)];

  const dom = {
    screens: $$('.screen'),
    navTabs: $$('.nav-tab'),
    monthsTabs: $('#months-tabs'),
    helpBtn: $('#help-btn'),
    modalOverlay: $('#modal-overlay'),
    modalClose: $('#modal-close'),
    start: {
      subtitle: $('#quiz-subtitle'),
      rulesQuestions: $('#rules-questions'),
      rulesTime: $('#rules-time'),
      name: $('#player-name'),
      nameError: $('#name-error'),
      start: $('#btn-start'),
      monthLeaderboard: $('#btn-month-leaderboard'),
    },
    quiz: {
      number: $('#q-number'),
      total: $('#q-total'),
      timer: $('#timer-box'),
      timerRing: $('#timer-ring'),
      timerValue: $('#timer-value'),
      toast: $('#toast'),
      question: $('#question-text'),
      options: $('#options'),
      progress: $('#progress'),
    },
    result: {
      accFill: $('#acc-fill'),
      accNum: $('#acc-num'),
      grade: $('#res-grade'),
      note: $('#res-note'),
      name: $('#res-name'),
      score: $('#res-score'),
      total: $('#res-total'),
      time: $('#stat-time'),
      streak: $('#stat-streak'),
      toBoard: $('#btn-to-board'),
    },
    leaderboard: {
      title: $('#leaderboard-title'),
      podium: $('#podium'),
      rows: $('#board-rows'),
      back: $('#btn-back-from-leaderboard'),
    },
    overall: {
      podium: $('#overall-podium'),
      rows: $('#overall-rows'),
      back: $('#btn-back-from-overall'),
    },
    archive: {
      grid: $('#archive-grid'),
      back: $('#btn-back-from-archive'),
    },
  };

  const formatTime = (s) => `${Math.floor(s / 60)}:${String(s % 60).padStart(2, '0')}`;
  const escapeHtml = (v) => String(v).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const getInitials = (name) => name.split(/\s+/).map(w => w[0]).join('').slice(0, 2).toUpperCase();

  function capitalizeName(raw) {
    return raw.split(/\s+/).slice(0, 2).map(w => w[0].toUpperCase() + w.slice(1).toLowerCase()).join(' ');
  }

  function animateNumber(el, target, { duration = 700, format = (v) => v } = {}) {
    const start = performance.now();
    const tick = (now) => {
      const p = Math.min((now - start) / duration, 1);
      const eased = 1 - Math.pow(1 - p, 3);
      el.textContent = format(Math.round(target * eased));
      if (p < 1) requestAnimationFrame(tick);
    };
    requestAnimationFrame(tick);
  }

  function showScreen(name) {
    dom.screens.forEach(s => s.classList.toggle('is-active', s.dataset.screen === name));
    window.scrollTo(0, 0);
  }

  function setActiveNav(tab) {
    dom.navTabs.forEach(t => t.classList.toggle('is-active', t.dataset.nav === tab));
  }

  function toggleQuizElements(show) {
    dom.monthsTabs.style.display = show ? 'flex' : 'none';
  }

  function openModal() {
    dom.modalOverlay.classList.add('is-open');
  }

  function closeModal() {
    dom.modalOverlay.classList.remove('is-open');
  }

  async function fetchFromSheets(action, params = '') {
    try {
      const url = `${GOOGLE_SHEETS_URL}?action=${action}${params}`;
      const response = await fetch(url);
      return await response.json();
    } catch (error) {
      console.error(`Ошибка загрузки (${action}):`, error);
      return null;
    }
  }

  async function initApp() {
    try {
      state.settings = await fetchFromSheets('settings');
      if (state.settings) {
        CONFIG.questionsPerGame = parseInt(state.settings['Вопросов в игре']) || 10;
        CONFIG.secondsPerQuestion = parseInt(state.settings['Секунд на вопрос']) || 25;
      }
    } catch(e) {
      console.log('Настройки не загрузились');
    }

    try {
      state.months = await fetchFromSheets('months') || [];
    } catch(e) { 
      state.months = []; 
    }

    state.activeMonth = state.months.find(m => m.status === 'Активен');
    state.nextMonth = state.months.find(m => m.status === 'Закрыт');

    if (!state.activeMonth) {
      state.activeMonth = { name: 'Август', year: 2026, status: 'Активен' };
    }
    if (!state.nextMonth) {
      state.nextMonth = { name: 'Сентябрь', year: 2026, status: 'Закрыт' };
    }

    try {
      state.questions = await fetchFromSheets('questions', `&month=${encodeURIComponent(state.activeMonth.name)}`) || [];
    } catch(e) { 
      state.questions = []; 
    }

    if (state.questions.length === 0) {
      console.error('❌ Вопросы не загрузились!');
    } else {
      console.log('✅ Загружено вопросов:', state.questions.length);
    }

    CONFIG.questionsPerGame = state.questions.length || CONFIG.questionsPerGame;
    dom.quiz.total.textContent = CONFIG.questionsPerGame;
    dom.result.total.textContent = `/${CONFIG.questionsPerGame}`;

    updateMonthsTabs();
    updateStartScreen();
  }

  function updateMonthsTabs() {
    const months = [state.activeMonth, state.nextMonth].filter(Boolean);
    dom.monthsTabs.innerHTML = months.map(m => {
      const isActive = m.status === 'Активен';
      const isLocked = m.status === 'Закрыт';
      return `
        <div class="month-tab ${isActive ? 'is-active' : ''} ${isLocked ? 'is-locked' : ''}">
          <div class="month-tab__name">${escapeHtml(m.name)}</div>
          <div class="month-tab__year">${escapeHtml(String(m.year))}</div>
          <div class="month-tab__status">${isActive ? 'Активна' : '🔒'}</div>
          ${isLocked ? '<div class="month-tab__tooltip">Викторина станет доступна в следующем месяце</div>' : ''}
        </div>`;
    }).join('');
  }

  function updateStartScreen() {
    if (state.activeMonth) {
      const monthName = state.activeMonth.name;
      dom.start.subtitle.textContent = `Ответь на вопросы месяца и займи место в рейтинге ${monthName.toLowerCase()}.`;
      dom.start.rulesTime.textContent = `На каждый вопрос — ${CONFIG.secondsPerQuestion} секунд`;
    }
  }

  function startGame() {
    if (!state.questions || state.questions.length === 0) {
      alert('Вопросы ещё загружаются. Подожди пару секунд и попробуй снова.');
      return;
    }

    const rawName = dom.start.name.value.trim();

    if (!CONFIG.namePattern.test(rawName)) {
      setNameError(true);
      dom.start.name.focus();
      return;
    }

    state.player = capitalizeName(rawName);
    state.index = 0;
    state.score = 0;
    state.spentSeconds = 0;
    state.history = [];
    state.streak = 0;
    state.bestStreak = 0;
    state.answers = [];

    buildProgressSegments();
    showScreen('quiz');
    renderQuestion();
  }

  function renderQuestion() {
    const question = state.questions[state.index];
    if (!question) {
      if (state.index >= state.questions.length && state.questions.length > 0) {
        finishGame();
      } else {
        console.error('❌ Вопросы не загрузились!');
        dom.quiz.question.textContent = 'Вопросы не загрузились. Проверьте таблицу.';
      }
      return;
    }

    state.locked = false;
    state.selected = null;

    dom.quiz.number.textContent = state.index + 1;
    dom.quiz.question.textContent = question.text;
    dom.quiz.options.innerHTML = question.options.map((option, idx) => `
      <button type="button" class="option" data-index="${idx}">
        <span class="option__key">${CONFIG.answerLetters[idx]}</span>
        <span class="option__text">${escapeHtml(option)}</span>
        <span class="option__icon" aria-hidden="true"></span>
      </button>`).join('');

    paintProgress();
    startTimer();
  }

  function buildProgressSegments() {
    dom.quiz.progress.innerHTML = state.questions.map(() => '<span class="progress__seg"></span>').join('');
  }

  function paintProgress() {
    $$('.progress__seg', dom.quiz.progress).forEach((seg, idx) => {
      seg.className = 'progress__seg';
      if (idx < state.history.length) {
        seg.classList.add(state.history[idx] ? 'progress__seg--ok' : 'progress__seg--fail');
      } else if (idx === state.index) {
        seg.classList.add('progress__seg--current');
      }
    });
  }

  function showToast(kind, text) {
    const toast = dom.quiz.toast;
    clearTimeout(state.toastId);
    toast.className = `toast toast--${kind}`;
    toast.textContent = text;
    void toast.offsetWidth;
    toast.classList.add('is-visible');
    state.toastId = setTimeout(() => toast.classList.remove('is-visible'), 1100);
  }

  function setNameError(visible) {
    dom.start.name.classList.remove('field--invalid');
    if (visible) {
      void dom.start.name.offsetWidth;
      dom.start.name.classList.add('field--invalid');
    }
    dom.start.nameError.classList.toggle('is-visible', visible);
  }

  function startTimer() {
    stopTimer();
    state.timeLeft = CONFIG.secondsPerQuestion;
    dom.quiz.timer.classList.remove('timer--warn');
    updateTimerUi();
    restartTimerRing();

    state.timerId = setInterval(() => {
      state.timeLeft -= 1;
      state.spentSeconds += 1;
      updateTimerUi();

      if (state.timeLeft <= CONFIG.warningThreshold) dom.quiz.timer.classList.add('timer--warn');
      if (state.timeLeft <= 0) confirmAnswer(TIMEOUT);
    }, 1000);
  }

  function stopTimer() {
    if (state.timerId !== null) {
      clearInterval(state.timerId);
      state.timerId = null;
    }
    freezeTimerRing();
  }

  const updateTimerUi = () => { dom.quiz.timerValue.textContent = state.timeLeft; };

  function restartTimerRing() {
    const ring = dom.quiz.timerRing;
    ring.style.transition = 'none';
    ring.style.strokeDashoffset = '0';
    void ring.getBoundingClientRect();
    ring.style.transition = `stroke-dashoffset ${CONFIG.secondsPerQuestion}s linear`;
    ring.style.strokeDashoffset = String(TIMER_C);
  }

  function freezeTimerRing() {
    const ring = dom.quiz.timerRing;
    ring.style.strokeDashoffset = getComputedStyle(ring).strokeDashoffset;
    ring.style.transition = 'none';
  }

  function handleOptionClick(index) {
    if (state.locked) return;

    if (state.selected === index) {
      confirmAnswer(index);
      return;
    }

    state.selected = index;
    $$('.option', dom.quiz.options).forEach(btn =>
      btn.classList.toggle('option--selected', Number(btn.dataset.index) === index));
  }

  function confirmAnswer(chosenIndex) {
    if (state.locked) return;

    state.locked = true;
    stopTimer();

    const question = state.questions[state.index];
    const isCorrect = chosenIndex === question.correctIndex;
    const timeSpent = CONFIG.secondsPerQuestion - state.timeLeft;

    state.answers.push({
      questionNumber: state.index + 1,
      questionText: question.text,
      userAnswer: chosenIndex === TIMEOUT ? 'Время вышло' : question.options[chosenIndex],
      correctAnswer: question.options[question.correctIndex],
      isCorrect: isCorrect,
      timeSpent: timeSpent,
    });

    $$('.option', dom.quiz.options).forEach(btn => {
      const idx = Number(btn.dataset.index);
      btn.disabled = true;
      btn.classList.remove('option--selected');

      if (idx === question.correctIndex) btn.classList.add('option--correct');
      else if (idx === chosenIndex) btn.classList.add('option--wrong');
      else btn.classList.add('option--dim');
    });

    if (isCorrect) {
      state.score += 1;
      state.streak += 1;
      state.bestStreak = Math.max(state.bestStreak, state.streak);
    } else {
      state.streak = 0;
    }
    state.history.push(isCorrect);
    paintProgress();

    if (chosenIndex === TIMEOUT) showToast('warn', TOASTS.timeout);
    else if (isCorrect) showToast('ok', TOASTS.correct);
    else showToast('fail', TOASTS.wrong);

    setTimeout(() => {
      state.index += 1;
      if (state.index < state.questions.length) renderQuestion();
      else finishGame();
    }, CONFIG.answerRevealDelay);
  }

  function finishGame() {
    stopTimer();

    const total = state.questions.length;
    if (total === 0) {
      console.error('❌ Нет вопросов!');
      showScreen('start');
      return;
    }

    const accuracy = Math.round((state.score / total) * 100);
    const grade = GRADES.find(g => accuracy >= g.min) || GRADES[GRADES.length - 1];

    dom.result.grade.textContent = grade.title;
    dom.result.note.textContent = grade.note;
    dom.result.name.textContent = state.player;
    dom.result.time.textContent = formatTime(state.spentSeconds);
    dom.result.streak.textContent = state.bestStreak;

    animateNumber(dom.result.score, state.score);
    animateNumber(dom.result.accNum, accuracy, { duration: 1100, format: v => `${v}%` });
    animateAccuracyRing(accuracy);

    sendToGoogleSheets();
    showScreen('result');
    if (accuracy >= CONFIG.passThreshold) launchConfetti();
  }

  function animateAccuracyRing(accuracy) {
    const ring = dom.result.accFill;
    ring.style.transition = 'none';
    ring.style.strokeDashoffset = String(ACC_C);
    void ring.getBoundingClientRect();
    ring.style.transition = 'stroke-dashoffset 1.2s var(--ease)';
    ring.style.strokeDashoffset = String(ACC_C * (1 - accuracy / 100));
  }

  function sendToGoogleSheets() {
    const resultData = {
      action: 'submit',
      id: Date.now(),
      name: state.player,
      month: state.activeMonth?.name || '',
      score: state.score,
      totalQuestions: state.questions.length,
      timeSeconds: state.spentSeconds,
      bestStreak: state.bestStreak,
      answers: state.answers,
    };

    fetch(GOOGLE_SHEETS_URL, {
      method: 'POST',
      mode: 'no-cors',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(resultData),
    }).then(() => {
      console.log('✅ Данные отправлены');
    }).catch((error) => {
      console.error('❌ Ошибка отправки:', error);
    });
  }

  async function renderMonthLeaderboard() {
    setActiveNav('quiz');
    toggleQuizElements(false);
    showScreen('leaderboard');
    
    const monthName = state.activeMonth?.name || '';
    dom.leaderboard.title.textContent = `Рейтинг: ${monthName}`;
    
    const results = await fetchFromSheets('leaderboard', `&month=${encodeURIComponent(monthName)}`) || [];
    
    dom.leaderboard.podium.innerHTML = buildPodiumHtml(results.slice(0, CONFIG.podiumSize));
    
    const rest = results.slice(CONFIG.podiumSize);
    dom.leaderboard.rows.innerHTML = rest.map((entry, i) =>
      boardRowHtml(entry, CONFIG.podiumSize + i, entry.name === state.player)).join('');
  }

  async function renderOverallLeaderboard() {
    setActiveNav('overall');
    toggleQuizElements(false);
    showScreen('overall');
    
    const results = await fetchFromSheets('overall') || [];
    
    dom.overall.podium.innerHTML = buildPodiumHtml(results.slice(0, CONFIG.podiumSize));
    
    const rest = results.slice(CONFIG.podiumSize);
    dom.overall.rows.innerHTML = rest.map((entry, i) =>
      overallRowHtml(entry, CONFIG.podiumSize + i, entry.name === state.player)).join('');
  }

  async function renderArchive() {
    setActiveNav('archive');
    toggleQuizElements(false);
    showScreen('archive');
    
    const months = await fetchFromSheets('archive') || [];
    
    dom.archive.grid.innerHTML = months.map(m => `
      <div class="archive-month" data-month="${escapeHtml(m.name)}">
        <div class="archive-month__name">${escapeHtml(m.name)}</div>
        <div class="archive-month__year">${escapeHtml(String(m.year))}</div>
      </div>`).join('');
    
    $$('.archive-month').forEach(el => {
      el.addEventListener('click', async () => {
        const month = el.dataset.month;
        dom.leaderboard.title.textContent = `Рейтинг: ${month}`;
        const results = await fetchFromSheets('leaderboard', `&month=${encodeURIComponent(month)}`) || [];
        dom.leaderboard.podium.innerHTML = buildPodiumHtml(results.slice(0, CONFIG.podiumSize));
        const rest = results.slice(CONFIG.podiumSize);
        dom.leaderboard.rows.innerHTML = rest.map((entry, i) =>
          boardRowHtml(entry, CONFIG.podiumSize + i, entry.name === state.player)).join('');
        toggleQuizElements(false);
        showScreen('leaderboard');
      });
    });
  }

  function buildPodiumHtml(topThree) {
    const layout = [1, 0, 2];
    return layout.filter(rank => topThree[rank]).map(rank => {
      const entry = topThree[rank];
      const place = rank + 1;
      const mine = entry.name === state.player;
      const isOverall = entry.totalScore !== undefined;
      const value = isOverall ? entry.totalScore : entry.score;
      const label = isOverall ? 'очков' : 'верно';
      const time = entry.seconds ? ` · ${formatTime(entry.seconds)}` : '';
      return `
        <div class="podium__col podium__col--${place}">
          <div class="podium__card">
            <div class="podium__medal ${place > 1 ? `podium__medal--${place}` : ''}">${place}</div>
            <div class="podium__ava">${escapeHtml(getInitials(entry.name))}</div>
            <div class="podium__name">${escapeHtml(entry.name)}</div>
            <div class="podium__meta"><b>${value}</b> ${label}${time}</div>
            ${mine ? '<div class="podium__tag">вы</div>' : ''}
          </div>
          <div class="podium__bar"></div>
        </div>`;
    }).join('');
  }

  function boardRowHtml(entry, place, mine) {
    return `
      <div class="board__row${mine ? ' board__row--mine' : ''}">
        <div class="board__place">${place + 1}</div>
        <div class="board__name">${escapeHtml(entry.name)}${mine ? '<span class="board__tag">вы</span>' : ''}</div>
        <div class="board__num">${entry.score}</div>
        <div class="board__num">${formatTime(entry.seconds)}</div>
      </div>`;
  }

  function overallRowHtml(entry, place, mine) {
    return `
      <div class="board__row${mine ? ' board__row--mine' : ''}">
        <div class="board__place">${place + 1}</div>
        <div class="board__name">${escapeHtml(entry.name)}${mine ? '<span class="board__tag">вы</span>' : ''}</div>
        <div class="board__num">${entry.totalScore}</div>
        <div class="board__num">${entry.participations}</div>
      </div>`;
  }

  function launchConfetti(count = 90) {
    const colors = ['#E01B24', '#E7C69A', '#FFFFFF', '#A5121B'];
    const fragment = document.createDocumentFragment();

    Array.from({ length: count }, (_, i) => {
      const piece = document.createElement('div');
      piece.className = 'confetti';
      piece.style.left = `${Math.random() * 100}vw`;
      piece.style.background = colors[i % colors.length];
      piece.style.animationDelay = `${(Math.random() * 1.2).toFixed(2)}s`;
      if (i % 3 === 0) piece.style.borderRadius = '50%';
      fragment.appendChild(piece);
    });

    document.body.appendChild(fragment);
    setTimeout(() => $$('.confetti').forEach(p => p.remove()), 4600);
  }

  function bindEvents() {
    dom.helpBtn.addEventListener('click', openModal);
    dom.modalClose.addEventListener('click', closeModal);
    dom.modalOverlay.addEventListener('click', (e) => {
      if (e.target === dom.modalOverlay) closeModal();
    });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') closeModal();
    });

    dom.navTabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const nav = tab.dataset.nav;
        if (nav === 'quiz') {
          setActiveNav('quiz');
          toggleQuizElements(true);
          showScreen('start');
        } else if (nav === 'overall') {
          renderOverallLeaderboard();
        } else if (nav === 'archive') {
          renderArchive();
        }
      });
    });

    dom.start.start.addEventListener('click', startGame);
    dom.start.monthLeaderboard.addEventListener('click', renderMonthLeaderboard);
    dom.start.name.addEventListener('input', () => setNameError(false));
    dom.start.name.addEventListener('keydown', e => {
      if (e.key === 'Enter') startGame();
    });

    dom.quiz.options.addEventListener('click', e => {
      const btn = e.target.closest('.option');
      if (btn) handleOptionClick(Number(btn.dataset.index));
    });

    dom.result.toBoard.addEventListener('click', renderMonthLeaderboard);

    dom.leaderboard.back.addEventListener('click', () => {
      setActiveNav('quiz');
      toggleQuizElements(true);
      showScreen('start');
    });
    dom.overall.back.addEventListener('click', () => {
      setActiveNav('quiz');
      toggleQuizElements(true);
      showScreen('start');
    });
    dom.archive.back.addEventListener('click', () => {
      setActiveNav('quiz');
      toggleQuizElements(true);
      showScreen('start');
    });
  }

  function init() {
    dom.quiz.timerRing.style.strokeDasharray = String(TIMER_C);
    dom.quiz.timerRing.style.strokeDashoffset = '0';
    dom.result.accFill.style.strokeDasharray = String(ACC_C);
    dom.result.accFill.style.strokeDashoffset = String(ACC_C);

    bindEvents();
    initApp();
  }

  init();
})();
</script>
</body>
</html>
